using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlSet
{
    //The controlSet class should be created from the settingscontroller
    //It contains all data to set the keybinding for a specific player
    //According to that one controlSet for each player exists in an array inside the settingscontroller, which contains the controlsets of every player

    //TODO Write some sort of var-map/enum/dictionary thing to save one controlSet with the input.Keycode-action relation (maybe enum) the binding of keys is handled in the keybinder class


}
